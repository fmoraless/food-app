import React from 'react';
import { StyleSheet, View, Text } from 'react-native';

export const OrdersScreen = () => {
  return (
    <View>
      <Text>Orders Component</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
