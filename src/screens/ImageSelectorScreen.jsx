import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

export const ImageSelectorScreen = () => {
  return (
    <View>
      <Text>Image Selector</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
